# .yaml表示配置文件，对TripService的一些描述，以及暴露的接口

type: google.api.Service
config_verison: 3

http:
  rules:
  # selector代表服务的名称
  - selector: coolcar.TripService.GetTrip
  # method:get 暴露接口，URL参数为id
  # URL：/trip/{id} => 调用coolcar.TripService.GetTrip
    get: /trip/{id}